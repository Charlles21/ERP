program EPR;

uses
  System.StartUpCopy,
  FMX.Forms,
  ERP_Principal in 'ERP_Principal.pas' {Form1},
  Router in 'View\Router\Router.pas';

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TForm1, Form1);
  Application.Run;
end.
